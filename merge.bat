git push origin master

git checkout upm
git merge master --no-edit
git push origin upm

git checkout vulkan
git merge master --no-edit
git push origin vulkan

git checkout intent
git merge master --no-edit
git push origin intent

git checkout master
